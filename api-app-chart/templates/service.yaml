apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-app-chart.fullname" . }}
  labels:
    {{- include "api-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-app-chart.selectorLabels" . | nindent 4 }}
